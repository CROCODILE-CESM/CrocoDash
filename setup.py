from setuptools import setup
if __name__ == '__main__':
    setup(
      name='crocodile_dashboard',
      packages=['crocodile_dashboard'],
      version='0.1',
      package_dir={'crocodile_dashboard': 'crocodile_dashboard'},
    )