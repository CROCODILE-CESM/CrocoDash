[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "CrocoDash"
version = "0.1.0"

[tool.setuptools]
packages = ["CrocoDash"]

[tool.setuptools.package-dir]
"CrocoDash" = "CrocoDash"