name: Run CrocoDash Notebooks

on:
  workflow_dispatch:
  push:
  schedule:
    - cron: '0 6 * * *'
env:
  CIME_MACHINE: ubuntu-latest
  DIN_LOC_ROOT: /workspace
  SCRATCH: /workspace
  CIME_OUTPUT_ROOT: /workspace
  USER: ${{ github.actor }}
jobs:
  resolve-image:
    runs-on: ubuntu-latest
    permissions:
      packages: read
    outputs:
      tag: ${{ steps.get-tag.outputs.tag }}
    steps:
      - name: Get latest immutable nightly tag
        id: get-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG=$(gh api /orgs/crocodile-cesm/packages/container/crocodash/versions \
            --jq '[.[] | select(.metadata.container.tags[] | startswith("nightly-"))] | sort_by(.created_at) | last | .metadata.container.tags[] | select(startswith("nightly-"))' \
            | head -1)
          echo "tag=ghcr.io/crocodile-cesm/crocodash:$TAG" >> $GITHUB_OUTPUT

  run_notebooks:
    needs: resolve-image
    runs-on: gha-runner-crocodash
    container:
      image: ${{ needs.resolve-image.outputs.tag }}
      options: --pull always
    steps:
      - name: Configure Git Identity
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
      - name: Show package versions                                                                                                                                                           
        run: conda run -n CrocoDash conda list    
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: CrocoDash

      - name: Copy CrocoDash into container workdir
        run: |
          rm -rf $WORKDIR/CrocoDash
          cp -r $GITHUB_WORKSPACE/CrocoDash $WORKDIR/

      - name: Set CI case paths
        run: |
          sed -i "s|\"fill_in_id\"|\"$WORKDIR/ci_input\"|g" $WORKDIR/CrocoDash/demos/ci_code/path_to_datasets.json
          sed -i "s|\"fill_in_cd\"|\"$WORKDIR/ci_cases\"|g" $WORKDIR/CrocoDash/demos/ci_code/path_to_datasets.json
          sed -i "s|\"Checkout\"|\"$CESMROOT\"|g" $WORKDIR/CrocoDash/demos/ci_code/path_to_datasets.json

      - name: Inject paths into notebooks
        run: |
          cd $WORKDIR/CrocoDash/demos
          conda run -n CrocoDash python ci_code/inject_paths_into_notebooks.py

      - name: Create CI case
        run: |
          cd $WORKDIR/CrocoDash/demos
          conda run -n CrocoDash jupyter nbconvert --to notebook --execute --inplace \
            --ExecutePreprocessor.timeout=600 \
            ci_code/ci_case_setup.ipynb
      - name: Clear CrocoDash notebook outputs
        run: |
          cd $WORKDIR/CrocoDash/demos
          find gallery/notebooks/CrocoDash -name "*.ipynb" \
            ! -path "*/.ipynb_checkpoints/*" \
            ! -name "BGC.ipynb" \
            | sort \
            | xargs -I {} conda run -n CrocoDash \
                jupyter nbconvert --clear-output --inplace \
                {}
        # BGC isn't run cuz it uses data on GDEX that isn't accessible on the GHCR runner. We should consider adding a test BGC dataset to the inputdata in the future so we can run those tests here too.
      - name: Run CrocoDash notebooks
        run: |
          cd $WORKDIR/CrocoDash/demos
          find gallery/notebooks/CrocoDash -name "*.ipynb" \
            ! -path "*/.ipynb_checkpoints/*" \
            ! -name "BGC.ipynb" \
            | sort \
            | xargs -I {} conda run -n CrocoDash \
                jupyter nbconvert --to notebook --execute --inplace \
                --ExecutePreprocessor.timeout=600 \
                --TagRemovePreprocessor.enabled=True \
                --TagRemovePreprocessor.remove_cell_tags='["ci-skip"]' \
                {}
